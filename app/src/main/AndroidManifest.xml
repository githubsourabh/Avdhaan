<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.avdhaan">

    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Required to receive BOOT_COMPLETED - MOVED HERE -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Avdhaan"
        android:appCategory="productivity"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        tools:targetApi="31">

        <!-- Launch point -->
        <activity android:name=".OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Pre-MainActivity checkpoint -->
        <activity android:name=".FinalActivityBeforeMainActivity" />

        <!-- Main Activity -->
        <activity 
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Other activities -->
        <activity android:name=".SelectAppsActivity" />
        <activity android:name=".ScheduleListActivity" />
        <activity android:name=".ScheduleSetupActivity" />
        <activity android:name=".FinalOnboardingActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".GrantAccessActivity" />
        <activity android:name=".CongratsActivity" />
        <activity android:name=".UsageOnlyOptionActivity" />
        <activity android:name=".UsageAccessRequestActivity" />
        <activity android:name=".NotReadyActivity" />
        <activity android:name=".AppSelectionOnboardingActivity" />
        <activity android:name=".AppSelectionIntroActivity" />
        <activity android:name=".AppSelectionSuccessActivity" />
        <activity android:name=".ScheduleSetupSuccessActivity" />
        <activity android:name=".AccessGrantedActivity" />
        <activity android:name=".PermissionExplanationActivity" />
        <activity android:name=".AccessNotGrantedInfoActivity" />
        <activity android:name=".UsageAccessInfoActivity" />
        <activity android:name=".AccessDeniedInfoActivity" />
        <activity android:name=".UsageAccessSettingsActivity" />
        <activity android:name=".OnboardingAccessUsagePermDenied" />
        
        <activity
            android:name=".BlockScreenActivity"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:exported="true">
        </activity>
        
        <activity android:name=".onboarding.WelcomeActivity" />
        <activity android:name=".UsageAccessDeclinedInfoActivity" />
        <activity android:name=".PermissionIntroActivity" />
        <activity android:name=".ScheduleBlockingActivity" />
        <activity android:name=".UsageAccessPermissionActivity" />
        <activity android:name=".AppBlockerSettingsActivity" />
        <activity android:name=".NoUsageAccessMessageActivity" />
        <activity android:name=".AccessDeniedExplanationActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".AppUsageListActivity" />

        <!-- Accessibility service -->
        <service
            android:name=".AppBlockService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Boot Receiver -->
        <receiver android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Alarm Receivers -->
        <receiver android:name=".FocusAlarmReceiver" android:exported="true"/>
        <receiver android:name=".FocusEndReceiver" android:exported="false" />

    </application>
</manifest>